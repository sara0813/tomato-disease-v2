"""1단계 준비: PlantVillage 원본을 train/val/test로 분할한다.

data/raw/plantvillage/<class>/  →  data/processed/plantvillage/{train,val,test}/<class>/
비율은 config.SPLIT_RATIO, 시드는 config.SEED를 따른다(클래스별 층화 분할).
"""

# TODO: 구현
